{
  "name": "@corehub/push-relay",
  "version": "1.1.2",
  "private": true,
  "description": "Core Hub push relay: a Cloudflare Worker that delivers a self-hosted hub's notices to the official iOS and Android apps with the owner's APNs and FCM keys, which never leave it (ADR 0024).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
