# محرّر مرئي لسير العمل في صفحة الجدولة
المسؤول: twuijri · الفرع: feat/workflow-editor · الحالة: in-progress

## المشكلة والهدف
محرّك سير العمل يعمل في الخادم (#92): خطوات `agent` و`condition` و`delay` و`notify` و`approval`،
وقوالب `{{steps.<id>.output}}`، والتحقّق عند الحفظ، والتشغيل والإلغاء وإعادة التشغيل من عقدة
والسجلّ. لكن الويب لا يملك شاشة **ترسم** سير العمل: يشغّله ويوافق على خطواته فقط، وسير العمل
يُصنع عبر الـAPI.

الهدف: قسم «سير العمل» في صفحة الجدولة فيه لوحة رسم (إضافة عقد، سحب، وصل بحوافّ نجاح وفشل،
تحريك وتكبير، حذف، لوحة مفاتيح)، ونماذج لكل نوع خطوة، وتحقّق حيّ من الخادم تظهر أخطاؤه على
العقد، وإنشاء وتعديل ونسخ وحذف، و«شغّل» و«شغّل من هذه العقدة»، وعرض التشغيل على اللوحة.

## القرار والموافقات
(يُكتب مع التنفيذ.)

## العقد
(يُكتب مع التنفيذ.)

## الملفات والتأثير
(يُكتب مع التنفيذ.)

## الفحوص
```
لم تُشغَّل بعد.
```

## المخاطر والرجوع
(يُكتب مع التنفيذ.)

## التسليم والخطوة التالية
قيد التنفيذ.
