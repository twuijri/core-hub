# الأدوات أثناء التشغيل: الزرّ الذي يكشف السابقة يطويها أيضًا
المسؤول: twuijri · الفرع: fix/live-tools-fold · الحالة: review

## المشكلة والهدف
قال المالك (٢٠٢٦-٠٩-٢٣، بلقطتين): «اذا فتحتها وهو يشتغل ما فيه زر يرجع يطويها اما الي تخلص
فيه زر يطويها».

السبب: في المجموعة الحيّة كان زرّ «N قبلها» يضبط `earlier = true` ثم يختفي، ولا شيء يعيده.
المجموعة المنتهية عنصر `details` فيطويها سطرها نفسه.

## القرار والموافقات
- زرّ واحد في مكانه لا يختفي: «N قبلها» يكشف السابقة، ثم يصير «إخفاء N السابقة» ويطويها،
  مع السهم نفسه الذي في سطر المجموعة المنتهية (يدور حين تنفتح)، و`aria-expanded` للقارئ.

## العقد
لا تغيير.

## الملفات والتأثير
- `packages/web/src/chat/ToolCallCard.tsx`، `packages/web/src/styles/chat.css`.
- اللغتان: مفتاح جديد `tool.hide_earlier`.
- `packages/web/tests/tool-calls.test.tsx`: يكشف ثم يطوي ويعود إلى آخر أربع.

## الفحوص
```
web: Test Files 30 passed · Tests 385 passed
lint، prettier، pnpm typecheck، contracts:check-clients: نظيفة
```
- **الاختبار يلتقط العطل**: بإرجاع `ToolCallCard.tsx` إلى `main` يسقط بـ
  `toHaveAttribute("aria-expanded", "false")`، ومع الإصلاح يمرّ.

## المخاطر والرجوع
لا شيء يُذكر؛ واجهة فقط. الرجوع: الفرع وحده.

## التسليم والخطوة التالية
PR إلى `main`. الدمج للمالك.
