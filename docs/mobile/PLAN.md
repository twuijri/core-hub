# Core Hub Mobile — خطة البناء (فرع `mobile`)

الحالة: مسودة معتمدة من المالك (2026-09-19). الفرع `mobile` طويل الأمد في هذا
المستودع حتى النضج؛ كل تعديل في ميزات الويب/سطح المكتب يجب أن ينعكس هنا.

## ما تعلمناه من تطبيق Ekko الرسمي (تحليل APK v1.0.4، للفهم فقط، بلا نسخ كود أو أصول)
- **ليس تطبيقًا أصليًا:** مبني بإطار uni-app (Vue 3 داخل WebView عبر DCloud)،
  ثماني صفحات فقط (`index`, `login`, `login-devices`, `devices`, `bootstrap`,
  `about`, `change-password`, `location-picker`)؛ الواجهة كلها داخل صفحة
  واحدة تشبه الويب. لهذا يبدو «مثل الويب».
- **نظام تصميم خاص به** باسم `--ink-*` (بطاقة، إدخال، رسالة، كود، تمييز)
  بوضعين فاتح/داكن وتخصيص لوني، وخط 14px، وحواف آمنة. ليس نفس رموز واجهة
  الويب عندنا؛ الفكرة المهمة: **رموز مركزية واحدة** لا ثوابت متفرقة.
- **الصوت (STT):** يسجّل بـ `uni.getRecorderManager` (mp3 عبر LAME) ثم يرفع
  إلى `POST /api/studio/stt/transcribe` بحقول `provider` + ملف `audio`
  (`speech.mp3`, `audio/mpeg`)؛ يقرأ `/api/studio/stt/settings` أولًا ويرفض
  صراحة المزوّد `browser` (`app_stt_browser_provider_unsupported`). النص يُدرج
  في صندوق الرسالة (زر `composer-voice-button` بحالات starting/recording/
  stopping/transcribing) ولا يُرسل تلقائيًا. أي أن الفارق عن تطبيقنا ليس
  «داخل الجهاز»، بل: المسار الصحيح `/api/studio/*`، حقل `provider` دائمًا،
  صيغة صوت يقبلها كل مزوّد (mp3/wav)، وحالات واضحة للزر.
- **الاتصال:** `/api/auth/app-login` بعد مسح QR، حساب سحابي اختياري
  (`/api/app/auth/*` — غير موجود عندنا ولا نحتاجه)، ريلاي `/app-relay` مع
  `relay.ready` و`http.request` و`socket.open` (للسحابة فقط)، والسوكت
  `/chat-run` بنفس أحداثنا (`app.resume`, `approval.*`, `clarify.*`,
  `location/calendar/health.*`, `app.events.subscribe`, `app.event`).
- **اللغات:** 11 لغة بينها العربية مع RTL. **الإشعارات:** لا FCM/APNs؛ أحداث
  السوكت فقط.
- **الوكلاء:** أيقونات hermes/ekko/claude/codex/deepseek/grok/pi/opencode،
  ونقاط `/api/coding-agents` و`/api/agents/*` كما عندنا.

## معرّف التطبيق (قرار المالك 2026-09-19)
`com.twuijri.corehub` على المنصتين (Android `applicationId`، iOS
`PRODUCT_BUNDLE_IDENTIFIER`؛ اختبارات iOS `com.twuijri.corehub.tests`)، وفريق
آبل `58QWJ228ZE`. مساحة الأسماء في كوتلن تبقى `us.i3u.hermesstudio`.

## قاعدة الإصدار (قرار المالك 2026-09-19)
رقم نسخة تطبيق الجوال = رقم آخر إصدار للكور (يُقرأ من `package.json` في جذر
المستودع؛ لا يُكتب يدويًا في التطبيق). بناءات التست: `<نسخة الكور>-test.<رقم
التشغيل>`، ورقم البناء (`versionCode`/`CURRENT_PROJECT_VERSION`) = رقم التشغيل.
الإصدار الرسمي للجوال يصدر مع إصدار الكور بنفس الرقم.

## قرارات
1. أصلي (Kotlin/Compose + SwiftUI) كما بدأ المالك، لا WebView.
2. عقد واحد: HTTP من `docs/openapi.json` (`npm run openapi:generate`) +
   وثيقة السوكت `docs/mobile/SOCKET-CONTRACT.md` (تُشتق من
   `packages/client/src/api/studio/chat.ts` و`sockets/chat-run.ts`)؛ نماذج
   المنصتين تُولَّد/تُطابق منه، مع اختبارات عقد مشتركة (ملفات JSON ثابتة).
3. المسارات القانونية `/api/studio/*` فقط (لا `/api/hermes/*`).
4. نظام تصميم «Core Hub» مشترك: رموز الألوان/الزوايا/الخطوط/الأيقونات مأخوذة
   من واجهة الويب (`packages/client/src/styles`) ومكتوبة مرة واحدة لكل منصة،
   وبنية تنقل مطابقة للويب: دردشة، اتصالات الأجهزة، برامج الجهاز، مدير
   الوكلاء (للمشرف)، النماذج، الإعدادات (نفس الأقسام).
5. الصوت: تسجيل ثم `/api/studio/stt/transcribe` مع `provider` (ومزوّد
   `local` يدعم النص الجزئي عبر `local-stream`)، وإدراج النص في الصندوق.
   التعرف داخل الجهاز (Apple Speech / Android SpeechRecognizer) خيار إضافي
   عندما لا يوجد مزوّد على السيرفر.
6. الأخطاء تُعرض دائمًا (لا `try?`/`runCatching` صامتة) عبر شريط موحّد.
7. مسار التست: بناء APK تلقائي من فرع `mobile` إلى صفحة `latest-test`
   (نفس المستودع الخاص)، وiOS عبر Xcode محليًا حتى توفر حساب المطوّر.

## المراحل (كل مرحلة طلب دمج إلى `mobile`)
- M0 الاستيراد (هذا الالتزام): `clients/android`, `clients/ios`,
  `clients/mobile-docs`.
- M1 العقد والأساس: OpenAPI + وثيقة السوكت، طبقة شبكة واحدة لكل منصة على
  `/api/studio/*`، ربط QR عبر `app-login`، اختيار بروفايل ووكيل ونموذج.
- M2 نظام التصميم وبنية التنقل.
- M3 الدردشة: بث، موافقات/استيضاحات، مرفقات (`app-uploads`)، صوت، TTS،
  ملفات `device://`.
- M4 الجلسات والمجموعات وسير العمل والإعدادات.
- M5 الإشعارات (`app.events.subscribe` + خدمة أمامية على أندرويد).
- M6 الأجهزة وبرامج الجهاز.

## M1 Android — ما أُنجز (فرع `mobile-m1-android`، 2026-09-19)

كل التغييرات داخل `clients/android` فقط (بلا مساس بـ `clients/ios`):

1. **المسارات القانونية:** كل نداءات الاستديو في `HermesApi.kt` صارت على
   `/api/studio/*` (الجلسات والبحث والفئات والرسائل وطول السياق، غرف الدردشة
   الجماعية، STT، TTS، تنزيل الملفات، `chat-run/runs`، الاستخدام والأداء).
   أُزيل تفاوض «جرّب studio ثم ارجع إلى hermes» وبديل `stt/settings`. بقيت
   `profiles`/`config`/`available-models`/skills/plugins/mcp/kanban/jobs تحت
   `/api/hermes/*` لأنها قانونية هناك. روابط التنزيل القديمة في سجلات الرسائل
   ما زالت تُفكّ. حُدِّث `HermesApiContractTest` و`tools/mock-studio.py`.
2. **الربط عبر QR:** زر «مسح رمز QR» في شاشة الدخول (`zxing-android-embedded`
   4.3.0، إذن الكاميرا وقت الاستخدام) يقرأ JSON الرمز، يضبط عنوان السيرفر منه،
   ثم `POST /api/auth/app-login` مع `device_code` ثابت لكل تثبيت (UUID في
   `EncryptedSharedPreferences`) واسم الجهاز وماركته وطرازه. الأكواد
   400/401/403/409/410 تظهر برسائل واضحة. اسم المستخدم/كلمة المرور خيار ثانٍ.
3. **تجديد الرمز الصامت:** `POST /api/auth/app-refresh` عند الإقلاع إذا بقي أقل
   من 7 أيام أو مضى أكثر من 24 ساعة على آخر تجديد، ومرة واحدة بعد أي `401`
   مع إعادة الطلب (`HermesApi.onUnauthorized`). فشل التجديد بـ 401 = تسجيل
   خروج برسالة. الرمز وتاريخ الانتهاء ووقت التجديد ومعرّف الاتصال تُحفظ في
   `commit()` واحد. (نقطة `app-refresh` تُنفَّذ في السيرفر بالتوازي؛ الكود
   مكتوب على العقد المتفق عليه.)
4. **الصوت داخل الجهاز افتراضيًا:** `SpeechRecognizer` بنتائج جزئية تُدرج حيًا
   عند المؤشر وتُستبدل حتى النتيجة النهائية، بلا إرسال تلقائي. إعداد
   «الإدخال الصوتي: هذا الجهاز / سيرفر Core Hub» في الإعدادات ← الصوت. عند
   غياب المعرّف يُستخدم السيرفر تلقائيًا مع تنبيه. مسار السيرفر صُحّح:
   `profile-status` أولًا برسالة واضحة، تسجيل WAV 16 kHz أحادي 16-بت بـ
   `AudioRecord` مع ترويسة RIFF صحيحة، ثم multipart بحقول `provider`
   و`language` والملف `audio` (`voice.wav`, `audio/wav`)، و`no_speech_detected`
   يظهر كرسالة. حالات الزر: خامل/استماع/تفريغ/خطأ.
5. **إظهار الأخطاء:** في الملفات المعدّلة أُزيلت `runCatching{}.getOrNull()`
   الصامتة حول نداءات الشبكة (بحث الجلسات، الفئات، طول السياق، حالات التشغيل،
   ملحقات الاتصالات، تشغيلات/جداول سير العمل، قوائم محرر الجدولة، استطلاع
   Weixin) وتُوجَّه إلى `UiState.error`.

**التحقق (محليًا، Gradle 8.11.1 عبر wrapper في مجلد مؤقت لأن AGP 8.9.2 يرفض
Gradle 8.9 الموجود على الجهاز؛ JDK 17؛ SDK 35):**
`gradlew testDebugUnitTest assembleDebug` → `BUILD SUCCESSFUL`، 103 اختبار
وحدة، 0 فشل، 0 خطأ (منها الجديدة: `AppAuthTest` 7، `AppPairingTest` 3،
`VoiceInputTest` 5، و`HermesApiContractTest` 47 بعد التحديث)، وملف
`app-debug.apk` مبني. **لم يُختبر** على جهاز أو محاكي فعلي: مسح QR الحقيقي،
`app-refresh` على سيرفر حي، جودة التعرف داخل الجهاز، وتسجيل `AudioRecord`.

**ملاحظات/متابعة:** الدخول بكلمة المرور ما زال عبر `/api/auth/login` (بلا
انتهاء/تجديد)؛ يمكن لاحقًا توحيده عبر `app-login` بكلمة مرور ليحصل على
اتصال جهاز ورمز قابل للتجديد. وثيقة السوكت وOpenAPI (بقية M1) لم تُنجز هنا.
## M1 iOS (فرع `mobile-m1-ios`، 2026-09-19)
ما نُفِّذ في `clients/ios` فقط (غير مُتحقَّق منه بمترجم؛ لا يوجد Xcode على
جهاز التطوير — المالك يبني ويختبر في Xcode):
- **المسارات القانونية:** كل نداءات `/api/hermes/*` التي لها مقابل قانوني
  صارت `/api/studio/*` (الجلسات، الرسائل، البحث `sessions/search`،
  التصنيفات، الاستخدام، الأداء، القروبات `group-chat/rooms`، الملفات
  `files/download`، STT، TTS، `chat-run/runs`). بقيت البروفايلات والإعدادات
  والنماذج والمهارات والإضافات وMCP وكانبان والمهام تحت `/api/hermes/*`.
  أُزيل أي تفاوض studio-ثم-hermes. لم يعد الرمز يوضع في `?token=` أبدًا؛
  التنزيلات (ملفات الوكيل، ملفات مساحة العمل، مرفقات كانبان) تمر عبر
  `URLSession` بترويسة `Authorization` ثم تُفتح من نسخة محلية.
- **الاقتران بـ QR:** زر «Scan QR code» في شاشة الدخول (AVFoundation
  `AVCaptureMetadataOutput`)، تحليل الحمولة `hermes-studio.app-connection`
  في `AppConnectionQR` (مع اختبارات)، ثم `POST /api/auth/app-login` بـ
  `device_code` ثابت في Keychain واسم جهاز قابل للتعديل. الرمز وتاريخ
  انتهائه ومعرّف الاتصال تُحفظ معًا في عنصر Keychain واحد
  (`AppSessionRecord`). رسائل واضحة (عربي/إنجليزي) لحالات 400/401/403/409/410.
- **التجديد الصامت:** `POST /api/auth/app-refresh` عند الإقلاع إذا بقي أقل
  من 7 أيام أو مضى أكثر من 24 ساعة على آخر تجديد، ومرة واحدة بعد أي 401 مع
  إعادة الطلب الأصلي. 401 من التجديد نفسه = تسجيل خروج برسالة. منطق القرار
  دالة نقية `AppTokenRefreshPolicy` مع اختبارات.
  **ملاحظة:** هذا الفرع من السيرفر لا يحتوي بعد على مسار `app-refresh`؛
  الخطأ غير-401 لا يُسجّل الخروج ويبقي الرمز الحالي.
- **الصوت:** الافتراضي داخل الجهاز (`SFSpeechRecognizer` + `AVAudioEngine`،
  نتائج جزئية حية تُستبدل في صندوق الرسالة، لا إرسال تلقائي)، وخيار
  «خادم Core Hub» في الإعدادات ← الصوت. مسار الخادم: `profile-status` أولًا
  ثم تسجيل WAV 16kHz mono 16-bit ثم `stt/transcribe` متعدد الأجزاء بحقل
  `provider` والملف `audio`/`voice.wav`. حالات الزر: idle/listening/
  transcribing/error، وكل خطأ ظاهر.
- **إظهار الأخطاء:** في الملفات المعدَّلة استُبدل `try?` على نداءات الشبكة
  بـ `store.attempt { … }` الذي يعرض الخطأ في الشريط الموحّد.
- ملفات جديدة: `Core/AppSession.swift`, `Core/SpeechTranscriber.swift`,
  `Features/QRScannerView.swift`. المشروع يستخدم مجلدات Xcode المتزامنة
  فلا حاجة لإدخالات pbxproj يدوية.
- المتبقي للمالك في Xcode: البناء، تشغيل الاختبارات، تجربة الاقتران على
  جهاز حقيقي، والتأكد من أذونات الكاميرا/الميكروفون/التعرّف على الكلام.

## M2 Android — نظام التصميم وبنية التنقل (فرع `mobile-m2-android`، 2026-09-19)

كل التغييرات داخل `clients/android` فقط (بلا مساس بـ `clients/ios`)، وفق
`docs/mobile/DESIGN-SPEC.md`:

1. **رموز التصميم (مصدر واحد):** `ui/theme/CoreHubTokens.kt` يحمل لوحتي
   Pure Ink الفاتحة والداكنة (bg/border/accent/text/success/error/warning/info/
   msg/code/textOnAccent/splash)، ونسب الحالات (hover 6%، selected 12%، حدود
   الإدخال 18/32%)، وسلّم الخطوط (14 أساس؛ 16/600 عناوين؛ 13 تنقل/جلسة؛ 12
   مؤلف؛ 11 معلومات؛ 10/600 رؤوس المجموعات؛ mono للكود)، والزوايا
   (6/8/10/14/18/999/4/5)، والظلال والمقاسات. `CoreHubTheme` يسقطها على
   `ColorScheme`/`Typography`/`Shapes` في Material 3 ويتبع إعداد
   النظام/فاتح/داكن الموجود؛ `CoreHubIcons` أيقونات الويب الخطية بنفس المسارات.
   `Theme.kt` القديم حُذف، و`DesignSystem.kt` صار يقرأ الرموز. حقول الكتابة لا
   تنزل عن 16sp.
2. **بنية التنقل = الويب:** أُزيلت التبويبات السفلية الثلاث. شريط علوي بزر
   القائمة يفتح درجًا جانبيًا (انزلاق 250ms، تعتيم 40%) فيه: الشريط الأساسي
   (محادثة جديدة، بحث، ترابط الأجهزة، إدارة الوكلاء للمشرف الأعلى من
   `/api/auth/me`، النماذج)، ثم مبدّل المحادثة بأربعة مقاطع (محادثة، محادثة
   جماعية، مسار العمل، السجل)، ثم قائمة الجلسات، ثم التذييل (اختيار البروفايل،
   اختيار النموذج، تسجيل الخروج مع شريحة اسم المستخدم، نقطة الحالة متصل/غير
   متصل، «Core Hub v{الإصدار}» من `GET /health` → `webui_version`، ومبدّل
   اللغة). الأقسام الأربعة تتشارك الغلاف `HomeShell`. «إدارة الوكلاء» هي
   شاشة الوكيل السابقة وتحوي كل الأدوات (المهام، كانبان، القنوات، المهارات،
   الإضافات، MCP، بيئات التشغيل، مسارات العمل، Ekko hub، الملفات، السجلات،
   الاتصالات، الرحلة، الخطافات، الإحصاءات) إضافة إلى إعدادات الوكيل/الجلسات/
   الضغط التي كانت خلف «مزيد من الإعدادات» (أُزيلت). درج الإعدادات: السجلات،
   الاستخدام، الأداء (مشرف)، استخدام المهارات، المظهر، الحيوانات الأليفة،
   البروفايلات (مشرف)، الإعدادات. صفحة الإعدادات بتبويبات الويب بالترتيب
   (الحساب الحالي، إدارة الحسابات، خطافات الويب، العرض، البروكسي، الضغط،
   الخصوصية، النماذج) ثم «هذا الجهاز» و«حول». لا توجد روابط ميتة؛ لم تُلزم
   شاشة «قادم في مرحلة لاحقة» لأن لكل مدخل شاشة موجودة (الأداء والاستخدام
   يفتحان شاشة الإحصاءات نفسها).
3. **قائمة الجلسات = الويب:** مجموعات RECENT (العدد من ترس، 1–100، محفوظ على
   الجهاز) / المثبّتة (تثبيت لكل بروفايل على الجهاز كما في الويب) / التصنيفات
   (قائمة ⋯ لإعادة التسمية والحذف) / غير مصنّف، برؤوس 10/600 كبيرة مع سهم
   يدور. الصف بسطرين: دبوس، نقطة غير مقروء (رد اكتمل والشاشة في مكان آخر)،
   العنوان باتجاه المحتوى، الوقت HH:mm لليوم وإلا «Sep 18»، صورة الوكيل 18px
   بخريطة runtime→asset نفسها (`chat-agent-avatar.ts`) مع حلقة متحركة أثناء
   البث، شريحة البروفايل، وسم التصنيف؛ ضغط مطوّل 500ms يفتح القائمة (إعادة
   تسمية، تثبيت، تصنيف، أرشفة، حذف)؛ ✕ الحذف بشفافية 50%. الفرز والتجميع
   دالة نقية (`SessionGroups.kt`) مغطاة باختبار.
4. **رأس الدردشة:** العنوان 16/600 باتجاه المحتوى في الشريط العلوي، شريحة
   مساحة العمل (آخر مقطع من المسار) وشريحة بيئة التشغيل، وقائمة ⋯ (تحديث،
   محادثة جديدة، تفريع، إعادة تسمية، مساحة العمل، تصدير، أرشفة). الفقاعات
   والمحرّر يستخدمان الرموز (زاوية 10، خلفيات الرسائل، بطاقة المحرّر 18 مع
   الظل، حقل 16sp، أزرار حبّية 999، إرسال/ميكروفون 30px، مؤشر السياق 11 كهرماني
   فوق 80%). تفاصيل الفقاعة (ملخص الأدوات، التفكير، صف الإجراءات) مؤجلة إلى M3.
5. **الهوية:** الاسم «Core Hub»، أيقونة المشغّل من `core-hub-mark.svg` على
   لون الإقلاع (#f7f7f4 فاتح/#1a1a1a داكن)، الشعار المضمّن `logo.png` كعلامة
   داخل التطبيق حتى يُجلب شعار السيرفر، وصور الوكلاء الثمانية مضمّنة (SVG →
   vector drawable، وPNG مصغّرة إلى 128px). حُذف المجلد الفارغ `assets-src`.
6. **RTL:** كل الحواف المنطقية (start/end)، الأيقونات الاتجاهية AutoMirrored،
   اتجاه النص لكل سلسلة (`TextDirection.Content`)، الكود ومعرّفات النماذج
   والإصدار LTR قسرًا، والدرج ينزلق من الحافة البادئة. كل السلاسل الجديدة
   بالعربية والإنجليزية (`TranslationsTest`/`RtlTest` ناجحان).
7. **تقسيم `MainActivity.kt`:** `ui/theme`، `ui/navigation` (الدرج، الغلاف،
   درج الإعدادات)، `ui/sessions` (التجميع، القائمة، السجل، الوقت، صور الوكلاء)،
   `ui/chat` (المحادثة، الرأس، الفقاعات، المحرّر)، `ui/settings` (صفحة
   التبويبات). السلوك كما هو حيث لم يُذكر خلاف ذلك.

**التحقق (محليًا؛ Gradle 8.11.1 من توزيع wrapper موجود في `~/.gradle`، JDK 17،
SDK 35، `--offline`):** `gradle testDebugUnitTest assembleDebug` →
`BUILD SUCCESSFUL`، 111 اختبار وحدة، 0 إخفاق، 0 خطأ (منها الجديدة:
`SessionGroupsTest` 6، و`NavigationStructureTest` أعيدت كتابتها بـ 9 اختبارات
للبنية الجديدة، وتحديث `ChannelSchemaTest` و`IconSemanticsTest`)، وملف
`app-debug.apk` مبني (~25 MB). **دخان على المحاكي المحلي** (الصورة الوحيدة
المتاحة: Android TV API 33، 1920×1080 أفقي، بلا شاشة، مع `tools/mock-studio.py`):
الإقلاع، تخطي التعريف، الدخول بكلمة مرور، فتح الدرج، السجل بمجموعاته،
الضغط المطوّل وقائمته، فتح محادثة بفقاعاتها، إدارة الوكلاء، درج الإعدادات
وصفحة التبويبات وتبويب «هذا الجهاز»، ثم التبديل إلى العربية والدرج والسجل
بالاتجاه المعكوس — بلا أي انهيار في logcat؛ أيقونة المشغّل ظهرت بعلامة Core
Hub. الملاحظات التي أُصلحت من هذه الجولة: ترس الإعدادات في تذييل الدرج،
تمرير الشريط مع القائمة على الشاشات القصيرة، تذييل أضيق، تصغير عناوين
المقاطع لتتسع، وسقف 80% لفقاعة الوكيل. **لم يُتحقق** على هاتف حقيقي (شاشة
عمودية، لمس حقيقي، مشغّلات مختلفة) ولا المطابقة البصرية الدقيقة مع الويب.

**متابعة:** M3 تفاصيل الفقاعات (ملخص الأدوات، التفكير، صف الإجراءات)؛
إعادة التقاط لقطات README على البنية الجديدة؛ تطبيق البنية نفسها على iOS.
## M2 iOS — «اجعله Core Hub» (فرع `mobile-m2-ios`، 2026-09-19)
كل التغييرات داخل `clients/ios` فقط (بلا مساس بـ `clients/android`). لا يوجد
Xcode على جهاز التطوير؛ الكود مكتوب بحذر بـ Swift 5 / SwiftUI (iOS 17) ولم
يُترجَم محليًا — CI على macOS يبني ويشغّل الاختبارات بعد الدمج.

1. **رموز التصميم (مصدر واحد):** `Theme/CoreHubTokens.swift` يحل محل ثوابت
   `HermesTheme` الخمس: لوحة «Pure Ink» فاتح/داكن كقيم hex خام
   (`PaletteHex.light/.dark`، مختبَرة) وألوان ديناميكية (`Palette.*`)،
   نسب الحالات (hover 6%، selected 12%، حدود الإدخال 18/32%)، سلّم الخطوط
   (14 أساس؛ 16/600 عناوين؛ 13 تنقّل/جلسة؛ 12 مؤلف؛ 11 بيانات؛ 10/600
   رؤوس المجموعات؛ monospaced للكود؛ الإدخال لا يقل عن 16)، الزوايا
   (6/8/10/14/18/999/4/5)، الظلال، الحركة (150/250ms)، والتخطيط. صبغة
   التطبيق = `accent`. كل الملفات الملموسة تستخدم الرموز فقط.
2. **الأيقونات:** `Theme/IconPath.swift` محلّل مسارات SVG للجزء المستخدم في
   المواصفة (`M/m L/l H/h V/v C/c A/a Z` + دوائر ومستطيلات مدوّرة؛ الأقواس
   تتحول إلى منحنيات تكعيبية) مع اختبارات؛ `Theme/CoreHubIcons.swift`
   أيقونات الشريط والمقاطع والترس بحدّ 1.8 ورؤوس مدوّرة، والأيقونات
   الاتجاهية تنعكس في RTL؛ `CoreHubMarkView` علامة Core Hub المتجهة.
3. **بنية التنقل = الويب:** `Features/RootShell.swift` بدل التبويبات: شريط
   علوي بزر همبرغر ودرج جانبي `Features/SidebarDrawer.swift` (انزلاق 250ms،
   تعتيم 40%، سحب للإغلاق، سحب من الحافة للفتح). محتوى الدرج: الشريط الأساسي
   (محادثة جديدة، بحث، ترابط الأجهزة، إدارة الوكلاء للمشرف، النماذج)، مبدّل
   المحادثة بأربعة مقاطع (محادثة/جماعية/مسار العمل/السجل)، قائمة الوضع
   الحالي، والتذييل (بروفايل، نموذج افتراضي، تسجيل الخروج + اسم المستخدم،
   نقطة الاتصال من `GET /health`، «Core Hub v{إصدار الخادم}»، اللغة والمظهر،
   وترس يفتح درج الإعدادات: السجلات، الاستخدام، الأداء (مشرف)، استخدام
   المهارات، المظهر، الحيوانات الأليفة، البروفايلات (مشرف)، الإعدادات + رجوع).
   صفحة الإعدادات بترتيب الويب (الحساب الحالي، إدارة الحسابات (مشرف —
   placeholder «قادم في مرحلة لاحقة»)، خطافات الويب، العرض، البروكسي،
   الضغط، الخصوصية، النماذج) + قسمان «متقدم» و«أدوات مساحة العمل» يحفظان
   كل الشاشات السابقة (كانبان، المهام المجدولة، القنوات، الملفات، الرحلة،
   الوكيل العام، المهارات، الإضافات، MCP، Ekko، إصدارات وقت التشغيل،
   الاتصالات). حُذف `RootTabs` و`AgentHubView` (القائمة) و`ProfileMenu`.
4. **قائمة الجلسات = الويب:** `Features/SessionListView.swift` صفوف من سطرين
   (دبوس، نقطة غير مقروء، عنوان باتجاه لكل نص، وقت HH:mm لنفس اليوم وإلا
   «Sep 18» عبر `SessionTimeFormatter` المختبَر، صورة الوكيل 18pt بخريطة
   `chat-agent-avatar.ts`، شريحة البروفايل، وسم التصنيف)، ضغطة مطوّلة
   لقائمة السياق (إعادة تسمية، تثبيت، تصنيف، أرشفة، إعدادات، حذف)، سحب
   للحذف/الأرشفة و✕ بشفافية 50%. المجموعات: RECENT (1–100، افتراضي 10، ترس
   لتغيير العدد) → المثبّتة → التصنيفات → غير مصنّف، رؤوس 10/600 بأحرف كبيرة
   مع سهم. المنطق النقي في `Core/SessionGrouping.swift` والتفضيلات المحلية
   (التثبيت لكل بروفايل، الطي، العدد، غير المقروء) في
   `Core/SessionBrowserPrefs.swift` — كلاهما مختبَر.
5. **رأس الدردشة والصناديق:** العنوان 16/600 باتجاه النص + شريحة مساحة العمل
   (آخر جزء من المسار) + قائمة ⋯؛ فقاعات بلون `msg.user/assistant` وزاوية
   10، الصندوق بطاقة زاوية 18 مع ظل وإدخال 16pt وأزرار pill وزر إرسال 30pt؛
   مؤشر السياق «45.0k / 256.0k · متبقٍ 211.0k». داخل الفقاعات = M3.
6. **الهوية:** الاسم «Core Hub» (`CFBundleDisplayName` بالعربية والإنجليزية)،
   أيقونة التطبيق من `core-hub-mark.svg` على `#f7f7f4` (سكربت
   `Scripts/generate_app_icons.swift` أُعيدت كتابته لرسم العلامة؛ الصور
   المضمّنة رُسمت بنفس الهندسة عبر PIL على لينكس)، الشعار داخل التطبيق
   (`AppMark`/`CoreHubMarkView`)، وصور الوكلاء `Agent-*` في الأصول (SVG
   كمتجهات، PNG مصغّرة إلى 256px).
7. **RTL:** اتجاه التخطيط من لغة التطبيق؛ اتجاه المحتوى لكل نص
   (`DirectionalText`)، الكود/المسارات/معرّفات النماذج LTR دائمًا
   (`TechnicalText`)، مسافات منطقية فقط، والأيقونات الاتجاهية تنعكس.
8. **الاختبارات:** `HermesStudioTests/CoreHubDesignTests.swift` (24 اختبارًا):
   قيم اللوحة والخطوط والزوايا والظلال، المحلّل (أرقام سالبة مضغوطة، أوامر
   ضمنية، منحنيات، أقواس تنتهي عند نقطة النهاية وتبقى على الدائرة)، كل
   أيقونة ترسم داخل الإطار، التجميع والترتيب، منسّق الوقت، شريحة مساحة
   العمل، خريطة الصور، التفضيلات، علم المشرف، الأرقام المضغوطة.

**انحرافات مقصودة عن المواصفة:** شريحة مساحة العمل تستخدم `accent@6%` بدل
`white@5%` لأن الأخيرة غير مرئية على `#fafafa`؛ عرض الدرج ≤ 300pt
(84% من الشاشة) بدل 240 الثابت للشريط الجانبي على سطح المكتب؛ زر «الأداء» و
«الاستخدام» يفتحان الشاشة نفسها (`InsightsView`) لأنها تجمع الاثنين.

**ما يجب التحقق منه في Xcode (لم يُترجَم هنا):**
- البناء بلا أخطاء (`HermesStudio` + `HermesStudioTests`) وتشغيل الاختبارات
  الـ 43 + 24. النقاط الأعلى خطرًا: `navigationDestination(item:)` مع
  `$store.shellDestination`، `.containerRelativeFrame`، تعبيرات SwiftUI
  الطويلة في `SessionRowView`/`SidebarDrawer` (قد تحتاج تقسيمًا لو اشتكى
  المحلّل من «unable to type-check»).
- فتح/إغلاق الدرج بالسحب في LTR وRTL (الإزاحة مضروبة في إشارة الاتجاه)،
  والتعتيم 40%، ومدة 250ms.
- الصفوف: الاتجاه لكل عنوان (عربي داخل واجهة إنجليزية والعكس)، العرض الأقصى
  لوسم التصنيف، ضغطة مطوّلة → القائمة، السحب → حذف/أرشفة.
- الأيقونات المرسومة بالمسارات تظهر بحجمها ولا تنقلب إلا الاتجاهية.
- الأصول: `Agent-*.imageset` (SVG متجهة) و`CoreHubMark` تُحمّل، وأيقونة
  التطبيق الجديدة تظهر على الشاشة الرئيسية باسم «Core Hub».
- المظهر الفاتح/الداكن/النظام يبدّل ألوان `Palette` (UIColor ديناميكي)
  والتذييل يعرض إصدار الخادم من `/health`.

## M3 Android — مطابقة الدردشة مع واجهة الويب (فرع `mobile-m3-android`، 2026-09-19)

كل التغييرات داخل `clients/android` فقط (بلا مساس بـ `clients/ios`)، وفق
قسمي «Message row» و«Composer» في `docs/mobile/DESIGN-SPEC.md` وعقد
`/chat-run` في `packages/server/src/modules/studio/sockets/chat-run.ts`:

1. **صف الرسالة (`ui/chat/MessageRow.kt`):** فقاعة المستخدم في جهة النهاية
   بعرض أقصى 75% (خلفية `msg.user`، زاوية 10، حشو 10×14)؛ صف الوكيل بصورة
   22dp وتسمية المؤلف 12sp وعرض أقصى 80% مع فقاعة تلتف حول النص القصير؛
   إشعار النظام بحدّ تحذيري 3dp في جهة البداية؛ أمر الشرطة المائلة كشريحة
   mono؛ الخطأ بلون الخطأ على خلفية 6%؛ ونقاط نابضة قبل وصول أول نص.
   `ChatLine` حصل على `kind` (User/Assistant/System/Command/Error) و`key`
   ثابت لكل رسالة.
2. **بطاقة الأدوات:** «{count} أداة / {count} tools» برأس 30dp وسهم يدور 90°
   وأيقونة مفتاح ربط وحتى ثلاثة أسماء مفصولة بـ « · » (+N) وعلامة ✓ / ••• / ✕؛
   عند التوسيع سطر لكل أداة (11sp mono، معاينة، دوّار/نجاح/خطأ) يفتح أقسام
   التفكير/الوسائط/النتيجة من `tool.completed`/`tool.failed` مع إظهار
   `output_truncated`/`output_original_length`. إعداد «إظهار نداءات الأدوات»
   في قائمة ⚙ يخفي البطاقة (محفوظ على الجهاز).
3. **كتلة التفكير:** «💭 التفكير · لوحظ {duration} · {count} حرفًا» بخط 13sp
   مائل بشفافية 85% وقابلة للطي؛ تُغذّى من `reasoning.delta` و`thinking.delta`
   و`reasoning.available`، والمدة من بداية التشغيل حتى أول نص للإجابة.
4. **صف الإجراءات تحت كل رسالة:** تشغيل/إيقاف مؤقت للصوت (TTS من
   `POST /api/studio/tts/synthesize` لكل رسالة، وعند فشل السيرفر يقرأ محرك
   Android TextToSpeech النص نفسه مع تنبيه)، نسخ (مع إشعار «نُسخت الرسالة»)،
   الإشارة إلى الرسالة (اقتباس في المحرّر)، تفريع (`/fork` كما في الويب؛ لا
   يوجد مسار HTTP للتفريع في السيرفر)، والوقت 11sp. الأزرار 24dp بزاوية 6.
5. **المحرّر (`ui/chat/Composer.kt`):** بطاقة زاوية 18 بارتفاع أدنى 150dp،
   مؤشر السياق أعلى جهة النهاية بنص «{used} / {limit} · متبقٍ {rest}» عبر
   `formatTokens` المطابق للويب (≥1e6 → x.xM، ≥1e3 → x.xk) بلون كهرماني فوق 80%
   وشريط 42×4؛ حقل نص بلا إطار 16sp باتجاه المحتوى ولا يأخذ التركيز تلقائيًا؛
   شريط الأدوات [+ كاميرا/معرض/ملفات] [🧠 قوة التفكير] [⚙ الوضع الصوتي ·
   إظهار نداءات الأدوات · الإرسال] [النموذج بعرض أقصى 190dp من
   `/api/hermes/available-models`] … [ميكروفون 30dp = إدخال M1 الصوتي]
   [إرسال 30dp دائرة/مربع إيقاف أثناء البث]؛ تنطوي تسميات الحبوب إلى أيقونات
   تحت 380dp. المرفقات عبر الرفع المجزّأ (`AppUploads.kt` +
   `HermesApi.openAppUpload/appendAppUploadChunk/completeAppUpload/abortAppUpload`):
   `POST /api/studio/app-uploads` ثم `PUT …/chunks?offset=N` بجسم خام ≤256 KiB
   ثم `POST …/complete`، مع شريحة تقدّم قابلة للإلغاء (`DELETE`)، حد 50 MB،
   والرجوع إلى `/upload` القديم إذا ردّ السيرفر 404. «الإرسال» يستدعي
   `POST /api/studio/sessions/{id}/push-enabled` كما في الويب.
6. **بطاقات التشغيل (`ui/chat/RunCards.kt`):** الموافقات (مرة/الجلسة/دائمًا/
   رفض) والاستيضاحات (خيارات + نص حر) بطاقات داخل الدفق بدل الحوار المنبثق؛
   قائمة الرسائل في الانتظار مع «تشغيل التالي» (`insert_queued_run`)
   و«مقاطعة بهذه الرسالة» (`steer_queued_run`) و«إلغاء» (`cancel_queued_run`)؛
   لافتات ضغط السياق (`compression.started/completed` مع الرموز قبل/بعد)
   والإيقاف (`abort.started/timeout/completed`)؛ `run.peer_user_message` يظهر
   كصف مستخدم؛ `session.command` سطر أمر (خطأ إن `ok=false`)؛
   `session.settings.updated` يحدّث حبوب النموذج/قوة التفكير/الإرسال؛
   `session.title.updated` و`session.workspace.updated` يحدّثان الرأس؛
   `message.interim` و`usage.updated` مدعومان.
7. **الملفات والوسائط:** روابط المسارات المطلقة و`device://<id>/<path>` في
   ردود الوكيل تتحوّل إلى بطاقات؛ الفيديو (mp4/webm/mov/m4v) يُشغَّل داخليًا عبر
   `VideoView` والصوت (mp3/wav/ogg/m4a/aac/flac) عبر `MediaPlayer` بترويسة
   `Authorization` (بلا رمز في الرابط، `HermesApi.streamUrl/mediaHeaders`) على
   `/api/studio/files/download` الذي يدعم HTTP ranges؛ بقية الملفات بطاقات تنزيل؛
   ملفات الجهاز تحمل وسم «على الجهاز».
8. **موافقات الجوال (`MobileLocation.kt`):** المصافحة ترسل `platform=android`؛
   عند `location.requested` يظهر حوار موافقة بالغرض والدقة، ثم إذن التشغيل،
   ثم `LocationManager` (بلا خدمات Google) يرد بـ `location.respond`
   `{ status: success|denied|error, location: { latitude, longitude,
   accuracyMeters, coordinateSystem: 'wgs84', timestamp } }` مع أكواد
   `location_permission_denied/unavailable/timeout/failed`. طلبات
   `calendar/reminder/health.requested` تُرفض فورًا بـ `denied` مع تنبيه
   (**TODO**: تكامل التقويم والتذكيرات والصحة على Android).
9. **الأخطاء والسلاسل:** كل الأخطاء تصل إلى `UiState.error` (بما فيها ملف لا
   يمكن قراءته من المنتقي، وحجم أكبر من 50 MB، وفقدان الرد على الموقع)؛ 44
   سلسلة جديدة بالعربية والإنجليزية؛ اتجاه المحتوى لكل نص والكود/الوسائط/
   المسارات LTR.

**التحقق (محليًا؛ Gradle 8.11.1 من توزيع wrapper في `~/.gradle`، JDK 17،
SDK 35، `--offline`):** `gradle testDebugUnitTest assembleDebug` →
`BUILD SUCCESSFUL`، 140 اختبار وحدة، 0 إخفاق، 0 خطأ (الجديدة:
`ChatFormatTest` 10 — المنسّقات وأنواع الوسائط وروابط الجهاز ونص النطق؛
`AppUploadsTest` 8 — تقسيم الأجزاء ومعرّفات الرفع وتسلسل open/chunk/complete/
abort على MockWebServer ومسار الإرسال ورابط البث بلا رمز؛ `ChatRunEventsTest`
11 — إعدادات الجلسة ورسالة النظير والضغط وأمر الجلسة وطلب الموقع ورد الموقع
وتفاصيل الأدوات و`usage.updated` وأنواع الأسطر)، وملف `app-debug.apk` مبني
(~24 MB). **دخان على المحاكي المحلي** (Android TV API 33، 1920×1080، بلا
شاشة، مع `tools/mock-studio.py` الذي لا يدعم Socket.IO فيُختبر مسار REST):
فتح محادثة جديدة بالعربية، إرسال رسالة من المحرّر الجديد، ظهور فقاعة المستخدم
في جهة النهاية وصف الوكيل بالصورة والتسمية، صفوف الإجراءات، فتح قائمة ⚙
بعناصرها الثلاثة، «الإشارة إلى الرسالة» تُظهر معاينة الرد، «نسخ» يُظهر
«نُسخت الرسالة» — بلا أي انهيار في logcat.
**لم يُتحقق** (يحتاج سيرفر حيًا أو هاتفًا حقيقيًا): البث الحي وبطاقة الأدوات
وكتلة التفكير بأحداث حقيقية، الرفع المجزّأ على السيرفر الفعلي، TTS من السيرفر
ومشغّلات الوسائط داخل الفقاعة (بترويسة التفويض وHTTP ranges)، ملفات
`device://`، حوار الموقع وقراءة `LocationManager` على هاتف، الموافقات
والاستيضاحات وقائمة الانتظار والضغط والإيقاف بأحداث حقيقية، وانطواء الحبوب
على شاشة ضيقة.

**قيود معروفة/متابعة:** سوكت `/chat-run` يُفتح لكل تشغيل ويُغلق بعده، لذا
`session.settings.updated` و`run.peer_user_message` و`location.requested`
تصل فقط أثناء تشغيل نشط من هذا الهاتف (سوكت جلسة دائم = M5 مع الإشعارات)؛
لا مقبض سحب لتغيير ارتفاع المحرّر؛ الإيقاف المؤقت للصوت متاح لصوت السيرفر
فقط (محرك الجهاز يتوقف بدل الإيقاف المؤقت)؛ مشغّل الفيديو هو `VideoView`
(لا ExoPlayer/Media3 لأن البناء بلا شبكة ولا توجد نسخة في ذاكرة Gradle)؛
التقويم/التذكيرات/الصحة مرفوضة كبديل مؤقت.
## M3 iOS — الدردشة بمطابقة عميل الويب (فرع `mobile-m3-ios`، 2026-09-19)
كل التغييرات داخل `clients/ios` فقط (بلا مساس بـ `clients/android`). لا يوجد
Xcode على جهاز التطوير؛ الكود Swift 5 / SwiftUI (iOS 17) مكتوب بحذر وغير
مُترجَم محليًا — CI على macOS يبني ويشغّل الاختبارات بعد الدمج.

1. **سوكت دائم `/chat-run`:** `Core/SocketIO.swift` أُعيد بناء `ChatSocket`
   ليبقى متصلًا طوال بقاء المحادثة على الشاشة (لا اتصال لكل تشغيل): مصافحة
   `auth: { token }` و`query: { profile, platform: 'ios' }` (تسجّل الهاتف كجهاز
   هدف)، `app.resume` عند كل اتصال، إعادة اتصال بتراجع أسّي حتى 30 ث، وإعادة
   الاتصال فورًا عند عودة التطبيق للواجهة. الترجمة من أحداث السيرفر إلى
   `LiveRunEvent` دالة نقية `ChatSocket.events(for:json:sessionID:)` تغطي:
   `message.delta/interim`، `reasoning.delta/thinking.delta/reasoning.available`،
   `tool.started/completed/failed` (مع `output_truncated`/`output_original_length`/
   `preview_truncated` عبر `ToolEvent`)، `subagent.*`/`delegation.updated`،
   `workspace.diff.completed`، `run.started/completed/failed/queued/
   queue_insertion.updated/peer_user_message/reattach_failed`،
   `approval.*`، `clarify.*`، `compression.*`، `abort.started/timeout/completed`،
   `usage.updated`، `session.command` (نهائي ما لم يكن `terminal === false`)،
   `session.title.updated`، `session.workspace.updated`،
   `session.settings.updated`، `resumed`، `location.requested`،
   `calendar/reminder/health.requested`. الإرسال: `run` (بحقل `push_enabled`
   الحالي)، `abort`، `approval.respond` (مع خيار `deny` الموجود)،
   `clarify.respond`، `insert/steer/cancel_queued_run`، `location.respond`،
   ورد `denied` على calendar/reminder/health.
2. **مخفِّض نقي للبث:** `Core/ChatStream.swift` — `ChatStreamState` +
   `ChatRunReducer.apply(event, to:)` يبني الأسطر (مستخدم/وكيل/نظام/أمر/خطأ/
   تفاعل)، النص المؤقت `interim`، التفكير مع وقت البداية/النهاية، الأدوات
   (إدراج/تحديث، مدة، مقاطعة عند الإيقاف)، الطابور، الضغط، الإيقاف، رسائل
   الأقران، الأوامر، الإعدادات، الموقع. زائد `ToolSummary` (≤3 أسماء فريدة
   «·» +N)، `ThinkingFormat` (5s / 1m 20s)، `ReferenceQuote` (8 أسطر `> `)،
   `ContextUsageFormat` (x.xk / x.xM، تحذير > 80%)، وخيارات جهد الاستدلال.
3. **الفقاعات (`Features/Chat/MessageRow.swift`):** مستخدم بحد 75% وخلفية
   `msg.user` وزاوية 10 وحشوة 10×14 وشرائح المرفقات؛ وكيل بصورة الوكيل 22pt
   وتسمية 12pt وبحد 80%؛ نظام بحد بادئ 3pt تحذيري؛ أمر (`/fork`) بخط أحادي على
   خلفية الكود؛ خطأ بلون الخطأ على `error@6%`؛ نقاط بث عند غياب النص.
4. **بطاقة الأدوات (`ToolSummaryCard.swift`):** رأس 30pt (سهم يدور 90°،
   مفتاح ربط من مسار الويب، «N tools/{count} أداة»، الأسماء، ✓ / ••• / Error)،
   وأسطر 11pt أحادية (حالة، اسم، معاينة، مدة) تُفتح إلى Thinking/Arguments/
   Result مع تنويه الاقتطاع. تحترم «إظهار نداءات الأدوات».
5. **كتلة التفكير (`ThinkingBlock.swift`):** 💭 Thinking/التفكير · Observed
   {duration} · {count} chars، 13pt مائل 85%، قابلة للطي، تحدّث كل ثانية أثناء البث.
6. **صف الإجراءات (`MessageActionRow.swift`، ظاهر دائمًا):** تشغيل/إيقاف مؤقت
   للصوت، نسخ، إشارة (اقتباس إلى المحرّر)، تفريع (`/fork` كما في الويب)،
   والوقت 11pt؛ أزرار 24pt زاوية 6.
7. **المحرّر (`ChatComposer.swift`):** بطاقة زاوية 18 وحد أدنى 150pt وظل؛ شريط
   [+ إرفاق: كاميرا / مكتبة الصور / الملفات] [🧠 جهد الاستدلال: default/none/
   minimal/low/medium/high/xhigh/max] [⚙: الوضع الصوتي، إظهار نداءات الأدوات ✓،
   الإشعارات ✓] [النموذج ≤190pt من `/api/hermes/available-models`] … [ميكروفون
   30pt = إدخال M1] [إرسال 30pt دائرة/مربع إيقاف أثناء البث]، وزر طابور عند
   وجود مسودة أثناء البث؛ مؤشر السياق أعلى النهاية؛ التسميات تنطوي إلى أيقونات
   دون 380pt؛ إدخال 16pt باتجاه لكل نص؛ لا تركيز تلقائي عند الفتح.
   `session.settings.updated` يُطبَّق على الحبّات.
8. **المرفقات المجزّأة (`Core/AppUploads.swift`):** `POST /api/studio/app-uploads`
   بمعرّف يولّده العميل (32 حرفًا من `[A-Za-z0-9_-]`) → `PUT …/chunks?offset=N`
   بجسم خام ≤ 256 KiB → `POST …/complete` → المسار إلى كتلة المحتوى؛ إلغاء عبر
   `DELETE`؛ تقدّم لكل مرفق وسقف 50 MB. الالتقاط بالكاميرا
   (`UIImagePickerController`)، مكتبة الصور (`PhotosPicker`)، والملفات
   (`fileImporter`) في `AttachmentPickers.swift`.
9. **الوسائط والملفات (`MediaPlayers.swift`، `Core/MediaLinks.swift`):** روابط
   المسارات المطلقة و`device://<id>/<path>` تمر عبر
   `/api/studio/files/download?path=` بترويسة Bearer؛ فيديو (mp4/webm/mov/m4v)
   بـ `VideoPlayer` وصوت (mp3/wav/ogg/m4a/aac/flac) بمشغّل مضمّن، كلاهما على
   `AVURLAsset` بمفتاح `AVURLAssetHTTPHeaderFieldsKey`؛ بقية الملفات بطاقات
   تنزيل؛ ملفات الجهاز تحمل وسم «On the device / على الجهاز».
10. **النطق (`Core/MessageSpeaker.swift`):** `POST /api/studio/tts/synthesize`
    مع `X-Hermes-Profile` → `AVAudioPlayer`، تشغيل/إيقاف مؤقت لكل رسالة، رسالة
    واحدة في كل مرة، تشغيل تلقائي للردود عند «الوضع الصوتي»، والرجوع إلى
    `AVSpeechSynthesizer` عند فشل الخادم.
11. **الموافقات والاستيضاحات داخل البث (`InteractionCard.swift`):** بطاقة
    بحد تحذيري: الأمر بخط أحادي، الوصف، أزرار الخيارات (once/session/always
    بحسب `allow_permanent`) + رفض، أو حقل إجابة حرّة مع `initial_response`
    و`response_mode`؛ تتحول إلى صف «تمت الموافقة/مرفوض/تمت الإجابة» عند الحل.
    الطابور (إدراج/توجيه/إلغاء)، لافتات الضغط والإيقاف وإعادة الاتصال،
    `run.peer_user_message` كسطر مستخدم، وصف تغييرات مساحة العمل في
    `ChatBanners.swift`.
12. **موافقة الجوال (`LocationConsentSheet.swift`، `Core/LocationConsent.swift`):**
    عند `location.requested` تظهر ورقة بالغرض والدقة؛ «مشاركة الموقع» تقرأ
    قراءة واحدة من CoreLocation (`NSLocationWhenInUseUsageDescription` في
    Info.plist) ثم `location.respond` `{ status:'success', location:{ latitude,
    longitude, accuracyMeters, coordinateSystem:'wgs84', timestamp } }`؛ «عدم
    المشاركة» أو الإغلاق → `denied`؛ الفشل → `error` مع الرمز والرسالة.
    **TODO:** طلبات `calendar.requested` / `reminder.requested` /
    `health.requested` تُرد `denied` تلقائيًا حتى تُبنى تكاملات EventKit/HealthKit.
13. **الأخطاء ظاهرة دائمًا:** كل فشل (تشغيل، رفع، نطق، موقع، طابور بلا اتصال)
    يظهر في الشريط الموحّد أو كسطر خطأ داخل البث. كل السلاسل الجديدة (72)
    بالعربية والإنجليزية في `Localizable.strings`؛ الاتجاه لكل نص، الكود
    والمسارات ومعرّفات النماذج LTR، المسافات منطقية.
14. **الاختبارات:** `HermesStudioTests/ChatParityTests.swift` (26 اختبارًا):
    مصافحة السوكت، تحليل أحداث الأدوات والاقتطاع، خريطة أحداث السيرفر،
    خيارات الموافقة، كتل المحتوى، المخفِّض (البث، الإكمال، الأدوات، الإيقاف،
    الفشل، التفاعلات، الأقران/الطابور/الإعدادات، الأوامر النهائية، الاستئناف،
    الطابور المتسلسل)، ملخص الأدوات، مدة التفكير، الاقتباس، مؤشر السياق،
    معرّفات وأجزاء الرفع، روابط الجهاز وأنواع الوسائط، سجل الرسائل، حمولة الموقع.

**انحرافات مقصودة:** المحرّر لا ينطوي إلى شريط صغير عند الفراغ (المواصفة:
حد أدنى 150pt)؛ بطاقة الأدوات تحمل خلفية `bgSecondary@60%` بدل
`rgba(surface,.28)`؛ عرض الفقاعات محسوب من `GeometryReader` واحد بدل
`containerRelativeFrame`.

**ما يجب التحقق منه في Xcode (لم يُترجَم هنا):**
- البناء (`HermesStudio` + `HermesStudioTests`) وتشغيل الاختبارات
  (43 + 24 + 26). النقاط الأعلى خطرًا: `MessageRowContext` بالإغلاقات داخل
  `LazyVStack`، `ConversationSheets` (تمرير `self` إلى `ViewModifier`)،
  `.photosPicker(isPresented:selection:maxSelectionCount:matching:)`،
  `TimelineView(.periodic)` داخل زر، تعبيرات `ChatComposer` الطويلة (قسّمها لو
  اشتكى المحلّل)، والتقاط `self` في إغلاقات `@Sendable` للرفع.
- السوكت: يبقى متصلًا بعد اكتمال الرد؛ سحب الشبكة ثم إعادتها → لافتة
  «إعادة الاتصال» ثم استئناف؛ الخلفية والعودة → اتصال فوري وتحميل السجل.
- البث: نقاط الانتظار ثم النص، كتلة التفكير بعدّاد الثواني، بطاقة «N tools»
  تفتح تلقائيًا أثناء التشغيل وتغلق بعده، زر الإيقاف (مربع) → لافتة
  «جارٍ الإيقاف…» ثم اكتمال؛ رسالة ثانية أثناء البث → زر الطابور → شريحة
  الطابور بإدراج/توجيه/إلغاء.
- الموافقة: بطاقة داخل البث، الخيارات ترسل `approval.respond`، الرفض `deny`،
  التحول إلى صف محلول؛ الاستيضاح: حقل الإجابة و`initial_response`.
- المرفقات: كاميرا/مكتبة/ملفات → شريحة بتقدّم وإلغاء (يحذف الجلسة على
  السيرفر)، الإرسال يتضمن كتل `image`/`file` بالمسار المُعاد؛ ملف > 50 MB يُرفض
  برسالة.
- الوسائط: رد يحوي `[clip](/path/clip.mp4)` يعرض مشغّل فيديو، `.m4a` مشغّل صوت
  بتقدّم، `device://…` يحمل وسم «على الجهاز»، بقية الملفات بطاقة تنزيل.
- النطق: زر التشغيل في صف الإجراءات (تشغيل/إيقاف مؤقت)، «الوضع الصوتي» ينطق
  الرد التالي تلقائيًا، وعند تعطيل TTS على الخادم يُستخدم صوت الجهاز.
- الموقع: أداة تطلب الموقع → ورقة الموافقة → إذن iOS → الرد بالإحداثيات؛
  الرفض/الإغلاق → `denied`.
- RTL/العربية: فقاعة المستخدم في النهاية، النص العربي بمحاذاة اليمين داخل
  فقاعة الوكيل، الأسهم والمفاتيح تنعكس، مؤشر السياق «45.0k / 256.0k · متبقٍ 211.0k»
  يبقى LTR، الحبّات تنطوي إلى أيقونات على iPhone SE.
- الوضعان الفاتح/الداكن: خلفيات `msg.*` و`bgComposer` والظلال.

## M4 iOS — الجلسات والمجموعات وسير العمل والإعدادات (فرع `mobile-m4-ios`، 2026-09-19)
كل التغييرات داخل `clients/ios` فقط (بلا مساس بـ `clients/android`). لا Xcode على
جهاز التطوير؛ الكود Swift 5 / SwiftUI (iOS 17) غير مُترجَم محليًا، وCI على macOS
يبني ويشغّل الاختبارات بعد الدمج. كل نداء يحمل `X-Hermes-Profile` عبر
`APIClient.activeProfile` الذي يتابع البروفايل المختار.

1. **الجلسات (الالتزام `5c1aa024`):** بحث في العناوين ونص الرسائل مع مقتطفات
   (`/api/studio/sessions/search`)، قائمة المؤرشفة من نقطة مجموعات السجل مع
   «إلغاء الأرشفة»، مرشّح «كل البروفايلات» يشترك فيه الدرج والسجل، وضع الاختيار
   الجماعي (أرشفة/استعادة/حذف/نقل إلى تصنيف)، قوائم ⋯ للتصنيفات، التصدير عبر
   ورقة المشاركة، إعدادات الجلسة (النموذج، جهد الاستدلال، الإشعار، مساحة العمل،
   التصنيف، الاستخدام، السياق الحالي)، وسجل مُصفَّح بـ «تحميل الأقدم»
   (`/messages/paginated`). منطق نقي: `SessionBatchSelection`، `MessagePaging`،
   `BatchResult`، `SaveState`.
2. **المحادثة الجماعية (`Features/GroupChat/`):** REST كامل في
   `Core/GroupChatAPI.swift` (الغرف، `config`/`workspace`/`invite-code`، المقاعد،
   الأعضاء، `clear-context`، الملخص، سلاسل التسليم، `rooms/join/{code}`،
   الإعدادات الجاهزة للوكلاء، المرفقات العادية والمجزّأة). سوكت `/group-chat`
   دائم لكل غرفة مع `join` بإقرار وإعادة اتصال أسّية، وخريطة أحداث نقية
   (`message`, `message_stream_start/delta/end`, `message_reasoning_delta`,
   `member_joined/left/kicked/updated`, `agents_updated`, `typing`,
   `room_agent_activity`, `execution_queue_updated`, `message_retracted`,
   `room_summary_updated`, `handoff_updated`, `approval.*`, `clarify.*`,
   `room_updated`, `room_cleared`, `context_status`). المخفِّض النقي
   `GroupRoomReducer`، ومحوّل `GroupRunLines` الذي يطوي رسائل تشغيل الوكيل
   الواحد (assistant + `tool` بنفس `run_id`) في سطر M3 واحد فيُعاد استخدام صف
   الرسالة وبطاقة الأدوات وكتلة التفكير. الشاشات: قائمة الغرف (صور الوكلاء، عدد
   الأعضاء، إنشاء، انضمام برمز، إعادة تسمية، نسخ، حذف)، شاشة الغرفة (نشاط كل
   وكيل مع المقاطعة، الكتابة، طابور التنفيذ، الموافقات والاستيضاحات داخل البث،
   سلاسل التسليم المتوقفة مع «متابعة»، تحميل الأقدم، محرّر بمرفقات وقائمة @)،
   وإعدادات الغرفة (الاسم، سياسة الملخص وعمق التسليم، مساحة العمل، رمز الدعوة مع
   QR من CoreImage ورابط المشاركة، المقاعد مع CRUD للإعدادات الجاهزة، الأعضاء،
   مسح السياق، الملخص).
   **قرار:** لا تُرسل حقول `mentions` المهيكلة؛ السيرفر يستخرج الإشارات من نص
   الرسالة للمرسل البشري ويرفض البيانات غير المطابقة، فقائمة @ تُدرج نصًا فقط.
3. **سير العمل (`Features/Workflow/`):** قائمة بشرائح حالة حيّة من سوكت
   `/workflow` (`WorkflowLiveStatuses` يملك `workflows.list` و
   `workflow.status.subscribe` بإقرار)، إنشاء، استيراد (معاينة → تأكيد → إلغاء)
   وحذف؛ شاشة المسار (تشغيل بمدخل وعقد بداية ومهلة، إيقاف، سجل التشغيلات،
   روابط الجدولة وملخص المخطط)؛ شاشة التشغيل (خط زمني للعقد يدمج جلسات العقد
   المحفوظة مع `nodeStatuses` الحيّة، الحالة والمدة والأخطاء، موافقة مضمّنة على
   العقد الموقوفة، عرض مخرجات العقدة من جلستها، إعادة التشغيل من عقدة)؛ الجدولة
   بوصف بشري لتعبير cron ومفتاح تفعيل/تعطيل؛ ملخص المخطط للقراءة فقط (لا محرّر
   على الهاتف — حُذف محرّر JSON القديم).
4. **الإعدادات بترتيب تبويبات الويب:** الحساب الحالي، إدارة الحسابات (مشرف أعلى،
   CRUD على `/api/auth/users`)، خطافات الويب (مشرف أعلى)، العرض (سمة الخادم
   والخلفية، لغة التطبيق، حجم النص على الجهاز، عرض الدردشة)، البروكسي، الضغط،
   الخصوصية، النماذج، ثم «هذا الجهاز» و«متقدم» و«أدوات مساحة العمل» و«حول».
   تبويب النماذج يقرأ `/api/hermes/available-models` ويحرّر النموذج الافتراضي
   ومجمّعات المزوّدين (المفتاح، الرابط، الاسم، مزوّد مخصص وحذفه) وقواعد الظهور
   والنماذج المخصصة والأسماء البديلة وحدود السياق. كل إعداد يعرض «جارٍ
   الحفظ…/تم الحفظ» أو نص خطأ الخادم. «حجم النص» صار يضرب خطوط الرموز فعليًا
   (`CoreHubTokens.Typography.scale`).
5. **Info.plist:** أُضيف `ITSAppUsesNonExemptEncryption = false` (HTTPS فقط، معفى)
   فلا يسأل TestFlight عن الامتثال التصديري في كل رفع (التزام منفصل).
6. **إصلاحات لازمة للترجمة:** `SocketIOConnection.emitWithAck` كان ينادي
   `emit(event, payload, ackID:)` بلا وسم `payload:` (خطأ ترجمة)، و`typingUsers`
   في لقطة الانضمام صارت تحفظ `userId` مع الاسم كي يزيل `stop_typing` المدخل
   الصحيح.
7. **السلاسل:** 224 مفتاحًا جديدًا في الإنجليزية و214 في العربية (الباقي كان
   موجودًا)، بصيغ `%lld`/`%@` المطابقة لما يولّده `String(localized:)`؛ الكود
   والمسارات ومعرّفات النماذج تبقى LTR، والمسافات منطقية.
8. **الاختبارات:** `HermesStudioTests/M4ParityTests.swift` (35 اختبارًا) تغطي
   المنطق النقي: خريطة أحداث الغرفة وتصفية الغرف الأخرى، لقطة الانضمام، المخفِّض
   (البث، الكتابة بالمعرّف، النشاط، الطابور، التسليم، التفاعلات، المسح، السجل
   الأقدم)، `GroupRunLines`، كتل المحتوى، `RoomAgentInput`/`RoomCreateDraft`/
   `RoomConfigDraft`، رابط الدعوة (مقارنة النص الكامل لا `URL.path`) وتوليد
   الرمز، قائمة @، أنماط حالة سير العمل، وصف cron، ترتيب المخطط والخط الزمني،
   غلاف إقرار سوكت سير العمل، كتالوج النماذج، أجسام إدارة الحسابات، تصفيح
   الرسائل والاختيار الجماعي، وقصّ حجم النص.

**ما يجب التحقق منه في Xcode (لم يُترجَم هنا):**
- البناء (`HermesStudio` + `HermesStudioTests`) وتشغيل الاختبارات
  (41 + 21 + 25 + 35 اختبارًا). النقاط الأعلى خطرًا: `RoomSheets` (تمرير `self` إلى
  `ViewModifier`)، الإغلاقات داخل `MessageRowContext` في `LazyVStack`،
  المُهيّئات المخصصة في `RoomSettingsView` و`RoomAgentEditorView`
  (`_state = State(initialValue:)`)، `@ObservedObject` المُمرَّر إلى
  `WorkflowRunView`، وتعبيرات `Form` الطويلة في `ModelCatalogView` (قسّمها لو
  اشتكى المحلّل).
- المجموعات: إنشاء غرفة (الملخص مطلوب من الخادم) → الانضمام برمز → إرسال رسالة
  و@إشارة لوكيل → بث الرد داخل صف M3 مع بطاقة الأدوات → موافقة مضمّنة →
  طابور التنفيذ والمقاطعة → «تحميل الأقدم» → إعدادات الغرفة (QR، مقعد جديد من
  إعداد جاهز، مسح السياق، تحرير الملخص) → طرد عضو ورؤية اللافتة على جهازه.
- سير العمل: شريحة الحالة تتغيّر حيًّا أثناء التشغيل، الإيقاف، موافقة عقدة
  موقوفة، «إعادة التشغيل من هنا»، عرض مخرجات عقدة، تفعيل/تعطيل جدولة.
- الإعدادات: إنشاء حساب وتعديله وحذفه (مشرف أعلى)، حفظ مفتاح مزوّد، إخفاء
  نموذج، اسم بديل، حد سياق، تغيير حجم النص وأثره على كل الشاشات.
- RTL/العربية: قائمة الغرف والخط الزمني ولوحات الطابور بالعربية، الأرقام
  والمعرّفات تبقى LTR، ورمز الدعوة والرابط LTR داخل نص عربي.
